@model Agenda.HomeIndexModel
@{
    ViewData["Title"] = "Home Page";
}

<input id="IsEventAddedSuccessfully" type="hidden" asp-for="IsAddingSuccessful" />
<table class="table">
    <thead>
        <tr>
            @{
                foreach (var item in Model.UrgencyList)
                {
                    switch (item.ObjectId)
                    {
                        case AgendaDataAccessLayer.EventUrgency.VeryUrgent:
                            <th scope="col" class="very-urgent display-6">
                                @item.Name
                            </th>
                            break;
                        case AgendaDataAccessLayer.EventUrgency.Urgent:
                            <th scope="col" class="urgent display-6">
                                @item.Name
                            </th>
                            break;
                        case AgendaDataAccessLayer.EventUrgency.Normal:
                            <th scope="col" class="normal display-6">
                                @item.Name
                            </th>
                            break;
                        case AgendaDataAccessLayer.EventUrgency.NotUrgent:
                            <th scope="col" class="not-urgent display-6">
                                @item.Name
                            </th>
                            break;
                        case AgendaDataAccessLayer.EventUrgency.NotUrgentAtAll:
                            <th scope="col" class="not-urgent-at-all display-6">
                                @item.Name
                            </th>
                            break;
                        default:
                            break;
                    }
                }
            }
        </tr>
    </thead>
    <tbody>
        <tr>
            @{
                <td class="very-urgent">
                    @foreach (var item in Model.EventList.Where(i => i.Event.EventUrgencyId == AgendaDataAccessLayer.EventUrgency.VeryUrgent).OrderByDescending(i => i.Event.EventDate).ThenBy(i => i.Event.Title))
                    {
                        <ul>
                            <li>
                                <input type="checkbox" id=@item.Event.ObjectId class="form-check-input">
                                <b>@item.Event.Title:</b> @item.Event.EventDate.ToString("dd-MM-yyyy")
                            </li>
                        </ul>
                    }
                </td>

                <td class="urgent">
                    <div style="text-align:left">

                        @foreach (var item in Model.EventList.Where(i => i.Event.EventUrgencyId == AgendaDataAccessLayer.EventUrgency.Urgent).OrderByDescending(i => i.Event.EventDate).ThenBy(i => i.Event.Title))
                        {
                            <ul>
                                <li>
                                    <input type="checkbox" id=@item.Event.ObjectId class="form-check-input">
                                    <b>@item.Event.Title:</b> @item.Event.EventDate.ToString("dd-MM-yyyy")
                                </li>
                            </ul>
                        }
                    </div>
                </td>

                <td class="normal">
                    @foreach (var item in Model.EventList.Where(i => i.Event.EventUrgencyId == AgendaDataAccessLayer.EventUrgency.Normal).OrderByDescending(i => i.Event.EventDate).ThenBy(i => i.Event.Title))
                    {
                        <ul>
                            <li>
                                <input type="checkbox" id=@item.Event.ObjectId class="form-check-input">
                                <b>@item.Event.Title:</b> @item.Event.EventDate.ToString("dd-MM-yyyy")
                            </li>
                        </ul>
                    }
                </td>
                <td class="not-urgent">
                    @foreach (var item in Model.EventList.Where(i => i.Event.EventUrgencyId == AgendaDataAccessLayer.EventUrgency.NotUrgent).OrderByDescending(i => i.Event.EventDate).ThenBy(i => i.Event.Title))
                    {
                        <ul>
                            <li>
                                <input type="checkbox" id=@item.Event.ObjectId class="form-check-input">
                                <b>@item.Event.Title:</b> @item.Event.EventDate.ToString("dd-MM-yyyy")
                            </li>
                        </ul>
                    }
                </td>
                <td class="not-urgent-at-all">
                    @foreach (var item in Model.EventList.Where(i => i.Event.EventUrgencyId == AgendaDataAccessLayer.EventUrgency.NotUrgentAtAll).OrderByDescending(i => i.Event.EventDate).ThenBy(i => i.Event.Title))
                    {
                        <ul>
                            <li>
                                <input type="checkbox" id=@item.Event.ObjectId class="form-check-input">
                                <b>@item.Event.Title:</b> @item.Event.EventDate.ToString("dd-MM-yyyy")
                            </li>
                        </ul>
                    }
                </td>
            }
        </tr>
    </tbody>
</table>
<div class="container done-event-div">
    <button id="done-event-button" type="submit" class="btn btn-dark">Done!</button>
</div>
